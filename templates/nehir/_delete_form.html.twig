<form method="post" action="{{ path('nehir_delete', {'id': nehir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nehir.id) }}">
    <button class="btn">Delete</button>
</form>
