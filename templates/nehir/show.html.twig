{% extends 'base.html.twig' %}

{% block title %}Nehir{% endblock %}

{% block body %}
    <h1>Nehir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nehir.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nehir.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nehir_index') }}">back to list</a>

    <a href="{{ path('nehir_edit', {'id': nehir.id}) }}">edit</a>

    {{ include('nehir/_delete_form.html.twig') }}
{% endblock %}
